services:
  libretv:
    image: bestzwei/libretv:latest
    container_name: libretv
    networks:
      - livetv
    ports:
      - "3022:8080"
    environment:
      - PASSWORD=admin123
    restart: always

networks:
  livetv:
    driver: bridge
    name: livetv